# Site URL
NEXT_PUBLIC_SITE_URL=https://valpha.dev

# SMTP Configuration for Contact Form
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@example.com
SMTP_PASS=your-app-password

# Email to receive contact form submissions (defaults to SMTP_USER if not set)
CONTACT_EMAIL=contact@valpha.dev
